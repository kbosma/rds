export interface OrganizationTheme {
  organizationThemeId: string;
  organizationId: string;
  primaryColor: string;
  accentColor: string;
  logoUrl: string | null;
  createdAt: string;
  createdBy: string;
  modifiedAt: string;
  modifiedBy: string;
}
